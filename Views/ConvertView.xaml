<UserControl x:Class="Cr_Interface.Views.ConvertView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cr_Interface.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource whiteBorder}" Margin="20 0 0 0">
        <Grid Margin="15 5">
            <StackPanel>

                <!--Title-->
                <TextBlock Text="Convert Currency" Style="{StaticResource titleText}"/>

                <TextBlock Text="Convert Currency Convert CurrencyConvert CurrencyConvert CurrencyConvert CurrencyConvert CurrencyConvert CurrencyConvert CurrencyConvert Currency" Style="{StaticResource secondaryText}"/>
                
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
